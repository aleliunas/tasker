# Tech Stack: Django + Docker

- Django 5.2.9 (LTS)
- Daphne as ASGI server.
- Docker Compose for all services (db, redis, web).
- Makefile is the primary entry point for commands.
- Use 'require' style for legacy JS, but focus is on Python/Django.
